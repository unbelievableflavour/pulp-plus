{
  const styles = `
    #search-wrap #results-list { 
      width: 600px;
    }
    #search-wrap #results-list li:not(.label), #select-options { 
      max-width: 100%; 
    }
  `;

  addStyle(styles);
}